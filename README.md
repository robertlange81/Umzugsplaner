# Umzugsplaner
adb shell 'setprop persist.sys.language de;setprop persist.sys.country DE;stop;sleep 5;start'
adb shell setprop persist.sys.language de;setprop persist.sys.country DE;stop;sleep 5;start
